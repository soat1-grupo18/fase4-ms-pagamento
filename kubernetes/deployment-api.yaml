apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-pagamento
  labels:
    name: ms-pagamento
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-pagamento
  template:
    metadata:
      labels:
        app: ms-pagamento
    spec:
      containers:
      - name: ms-pagamento
        image: pabloldias/soat1-grupo18-ms-pagamento:latest
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "384Mi"
            cpu: "2000m"
        env:
        - name: SERVER_PORT
          value: "8080"
        ports:
        - containerPort: 8080
